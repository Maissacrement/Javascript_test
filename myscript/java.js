class java
{ 

    // Propriete
    constructor(name, age)
    {
        this.name = name,
            this.age = age
    }

    say()
    {
        console.log("C'est moi " + this.name + " et j'ai " + this.age)
    }
}
